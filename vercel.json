{
  "buildCommand": "pnpm --filter ./artifacts/api-server run build && pnpm --filter ./artifacts/matka-app run build",
  "outputDirectory": "artifacts/matka-app/dist/public",
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
