{
  "name": "satta-matka-platform",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev:api": "pnpm --filter ./artifacts/api-server dev",
    "dev:app": "pnpm --filter ./artifacts/matka-app dev",
    "dev:admin": "pnpm --filter ./artifacts/admin dev",
    "build": "pnpm -r --if-present run build",
    "typecheck": "pnpm -r --if-present run typecheck"
  },
  "devDependencies": {
    "prettier": "^3.9.5",
    "typescript": "~5.9.3"
  }
}
